{{ form_start(form, {'attr': {'class': 'max-w-md mx-auto p-4 bg-white rounded-lg shadow-lg'}}) }}
    {{ form_widget(form) }}
    <button class="btn bg-blue-500 text-white font-bold py-2 px-6 mx-auto block">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}